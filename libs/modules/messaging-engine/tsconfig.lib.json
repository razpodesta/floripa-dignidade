{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "../../../dist/out-tsc/libs/modules/messaging-engine",
    "tsBuildInfoFile": "../../../dist/out-tsc/libs/modules/messaging-engine/tsconfig.lib.tsbuildinfo",

    /* --- INTEGRIDAD SOBERANA (ADN Emitter) --- */
    "composite": true,           /* Habilita la compilación incremental del Hub de comunicación */
    "declaration": true,         /* Emite las firmas de tipos (.d.ts) para el Portal y CMS */
    "declarationMap": true,      /* Permite el salto al código fuente desde el IDE */
    "emitDeclarationOnly": true, /* Delega la lógica de ejecución al motor SWC/Turbopack */
    "rootDir": "src",            /* Blindaje de frontera física absoluta (Fix TS6059) */

    /* --- RESOLUCIÓN NEXT-GEN (Next.js 16 Ready) --- */
    "module": "Preserve",
    "moduleResolution": "bundler",
    "verbatimModuleSyntax": true, /* Fuerza la separación estricta entre tipos y lógica */
    "isolatedModules": true,

    /* 🛡️ SANEADO: Soporte para interpretación de Hooks y componentes de UI de mensajería */
    "jsx": "react-jsx",

    /* --- ADN DEL ENTORNO (Communication Context) --- */
    "types": ["node"],
    "lib": ["DOM", "DOM.Iterable", "ES2022"] /* Soporte para Web Push, Fetch y Sockets */
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.json"],
  "exclude": [
    "src/**/*.spec.ts",
    "src/**/*.test.ts",
    "jest.config.ts",
    "dist",
    "out-tsc",
    "node_modules",
    "../../../dist",
    "../../../node_modules"
  ]
}
